You are generating EXACTLY ONE canonical vulnerability record from audited repository history.

Your job is to recover repository-grounded facts from the audited commit and surrounding code, not to generate a chat conversation.

You must:
1. inspect the pre-fix and post-fix repository state,
2. identify the primary vulnerable symbol,
3. resolve all identifiers necessary to explain exploitability and the fix,
4. recover inter-procedural context and control/data-flow,
5. reconstruct the full real fix scope,
6. extract the complete pre-fix and post-fix primary callable body,
7. untangle vulnerability-relevant hunks from supporting or unrelated hunks,
8. output exactly one valid JSON object following the schema below.

Treat the CVE and commit as grounding metadata, not as proof of the exact bug mechanism.
Do not invent identifiers, helper symbols, locations, definitions, vulnerable regions, or patch hunks.

====================
INPUT ROW
====================
- cve_id: {cve_id}
- commit_hash: {commit_hash}
- selection_source: {selection_source}
- repo_name: {repo_name}
- repo_url: {repo_url}
- commit_urls: {commit_urls}
- source_file: {source_file}

====================
DETERMINISTIC PATH RULES
====================
- Use deterministic paths only; never use random, timestamped, or user-home-derived temporary directories.
- Repository clone/checkout path MUST be exactly: `/home/user1/dataset_pipeline/repos/{cve_id}`
- If `/home/user1/dataset_pipeline/repos/{cve_id}` already exists, reuse it instead of cloning into a different path.
- Primary canonical JSON path MUST be exactly: `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`
- The filename must use the CVE ID exactly as provided (example: `CVE-2023-0215.json`).
- Do not write alternate final JSON filenames (no `_output`, `_canonical`, numbered, or random suffixes).
- Do not emit the full JSON object to stdout.

====================
FILE WRITE REQUIREMENTS
====================
- Persist the final canonical JSON to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` before finishing.
- Stdout-only completion is invalid.
- If file writing fails, retry until the write succeeds.
- The saved file must contain the complete canonical JSON object.

====================
LANGUAGE ASSUMPTION
====================
The target language is C# repository code.

The primary vulnerable unit may be any of:
- method
- constructor
- local function
- lambda expression
- anonymous delegate
- property getter/setter/init accessor
- indexer accessor
- event handler
- controller action
- minimal API handler
- middleware delegate
- extension method
- static helper
- async method
- iterator method

Use repository-grounded C# semantics. Do not force C/C++ assumptions onto the code.

====================
REPOSITORY ANALYSIS REQUIREMENTS
====================
- Resolve all identifiers required for exploitability or patch understanding:
  - namespaces and using directives
  - classes, records, structs, interfaces
  - methods, constructors, local functions, lambdas, delegates
  - properties, fields, indexers, events
  - enums and enum members
  - constants, readonly fields, static members
  - generic type parameters and constraints
  - attributes / annotations
  - async helpers, Task / ValueTask flows, cancellation paths
  - LINQ query chains, extension methods, expression trees
  - serializers, deserializers, model binders, validators, sanitizers
  - dependency injection registrations, service resolution, middleware pipeline, endpoint bindings
  - reflection helpers, dynamic invocation, unsafe blocks, Span/Memory usage, stream/pipeline helpers where relevant
- Recover the caller/callee chain relevant to exploitability
- Recover attacker-controlled input path where relevant
- Recover entry points where relevant
- Recover source-to-sink path where relevant
- Recover async sequencing where relevant
- Recover patch scope across all changed files/hunks
- If a symbol is still unresolved after exhaustive repository search, keep it in `unresolved_identifiers`
- Prefer exact repository-grounded definitions over paraphrases
- Prefer exact patch fidelity over explanatory creativity
- Do not over-resolve unrelated symbols; only include identifiers material to exploitability, control flow, data flow, or the fix

====================
IDENTIFIER DEFINITION RULES
====================
- Every identifier with kind = "method", "constructor", "lambda", "delegate", "property_accessor", "middleware", or "endpoint_handler" MUST include the COMPLETE callable body as the `definition` value, not just a name or summary.
- The `definition` field for methods/callables MUST be valid C# source code, not plain English.
- The `definition` field for classes, records, structs, interfaces, enums, constants, fields, properties, attributes, and using/namespace declarations MUST be the actual code definition, not an English description.
- `primary_symbol_pre` is a top-level attribute containing the full verbatim body of the primary vulnerable callable unit as it existed BEFORE the fix, including the vulnerable line(s).
- `primary_symbol_post` is a top-level attribute containing the full verbatim body of the primary vulnerable callable unit as it existed AFTER the fix, showing the patched line(s).
- Both `primary_symbol_pre` and `primary_symbol_post` must be complete, syntactically valid C# callable bodies extracted from the repository at `pre_state` and `post_state` respectively.
- Every `resolved_identifiers[]` item MUST include `state` with one of `pre`, `post`, or `both`.
- Do not collapse changed helper definitions across states into one entry; emit separate `state = "pre"` and `state = "post"` entries when the code differs.
- If the vulnerable logic is implemented as a lambda, local function, property accessor, middleware delegate, or anonymous delegate, extract that exact code form verbatim.

====================
PATCH UNTANGLING RULES
====================
- Do not assume every changed hunk in the fix commit is vulnerability-relevant.
- Separate the patch into:
  1. `security_fix_hunks`
  2. `supporting_hunks`
  3. `unrelated_hunks`
- A hunk is `security_fix_hunk` only if it directly changes the vulnerable condition, exploitability path, unsafe parser/serializer logic, validation logic, authorization logic, nullability/guard logic, bounds check, state reset, path normalization, escaping/encoding logic, reflection/dynamic dispatch safety, unsafe memory handling, async ordering, or other root-cause-bearing logic.
- A hunk is `supporting_hunk` if it is required for the security fix to preserve behavior, update dependent code, adjust service wiring, update endpoint/controller bindings, keep the program correct, or make the patch coherent, but is not itself the primary vulnerable region.
- A hunk is `unrelated_hunk` if it is test-only, formatting-only, analyzer-only, comment-only, logging-only, refactor-only, rename-only, dead-code-only, or otherwise not required to explain or fix the vulnerability.
- `vulnerable_line_ranges` must be compact inclusive ranges for the same pre-fix vulnerable region, formatted as `[[start_line, end_line], ...]`.
- Preserve the full patch diff in `patch.diff`, but separately identify which hunks are vulnerability-relevant.
- If a changed file contains both relevant and irrelevant hunks, do not mark the entire file as vulnerable; isolate the specific hunk or region.
- If uncertainty remains about whether a hunk is supporting vs unrelated, prefer `supporting_hunk` and explain why.

====================
STRICT OUTPUT RULES
====================
- Persist VALID JSON in `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No markdown fences
- After saving the file, stdout must be exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No extra top-level keys
- `unresolved_identifiers` must be an array, possibly empty
- `patch.diff` must be the complete unified diff for the true fix scope
- Use normalized severity:
  LOW | MEDIUM | HIGH | CRITICAL | NONE | UNKNOWN
- Use `vulnerable_line_ranges` as compact inclusive ranges `[[start_line, end_line], ...]` from the same pre-fix vulnerable region
- If the true fix spans multiple files, include all of them in `patch.files`
- `primary_symbol_pre` must be a non-empty string containing the complete pre-fix callable body
- `primary_symbol_post` must be a non-empty string containing the complete post-fix callable body
- All `resolved_identifiers[].definition` fields must contain actual code definitions, not English descriptions
- For each `resolved_identifiers[]` item with callable kinds, `function_body` must be present and non-empty
- `primary_vulnerable_region` must refer to the root-cause-bearing pre-fix code region only
- `patch_relevance` must be present even if one or more of its arrays are empty
- Every helper list must be state-specific (`*_pre` for `pre_state`, `*_post` for `post_state`) and must not merge states.
- Every helper listed in `*_pre` or `*_post` must have at least one matching `resolved_identifiers[]` entry with consistent `state`.
- Do not fabricate line numbers, type names, namespaces, service registrations, endpoint bindings, or diff hunks

====================
OUTPUT SCHEMA
====================
{
  "id": "<cve>__<file_path_sanitized>__<primary_symbol>",
  "cve_id": "<string>",
  "repo": "<string>",
  "repo_url": "<string>",
  "selection_source": "<string>",
  "language": "C#",
  "file_path": "<primary vulnerable file path>",
  "primary_symbol": "<primary vulnerable callable or symbol name>",
  "primary_symbol_kind": "<method|constructor|local_function|lambda|delegate|property_accessor|event_handler|controller_action|minimal_api_handler|middleware|extension_method|static_helper|async_method|iterator_method>",
  "helper_symbols_pre": ["<symbol_from_pre_state>", "..."],
  "helper_symbols_post": ["<symbol_from_post_state>", "..."],
  "helper_symbol_pairs": [
    {
      "pre": "<symbol_from_pre_state_or_empty>",
      "post": "<symbol_from_post_state_or_empty>",
      "change": "<unchanged|modified|renamed|added|removed>"
    }
  ],
  "namespace_context": "<namespace or namespace path>",
  "runtime_context": {
    "platform": "<aspnet|desktop|service|library|unity|blazor|unknown>",
    "framework": "<aspnetcore|efcore|wpf|winforms|xamarin|maui|plain|unknown>",
    "execution_model": "<sync|async|mixed>"
  },
  "target_framework": "<net48|net6.0|net8.0|unknown>",
  "commit": "<fix commit sha>",
  "pre_state": "<parent commit sha>",
  "post_state": "<fix commit sha>",
  "cwe": "<CWE-ID or UNKNOWN>",
  "severity": "<LOW|MEDIUM|HIGH|CRITICAL|NONE|UNKNOWN>",
  "vulnerable": <true|false>,
  "vulnerable_line_ranges": [[<int>, <int>], "..."],
  "primary_symbol_pre": "<full callable body from pre-fix version>",
  "primary_symbol_post": "<full callable body from post-fix version>",
  "primary_vulnerable_region": {
    "file": "<path>",
    "lines": [<int>, "..."],
    "reason": "<precise explanation of why this is the root-cause-bearing pre-fix region>"
  },
  "root_cause": "<precise repository-grounded explanation>",
  "impact": "<short impact summary>",
  "exploitability_summary": "<short exploitability summary>",
  "attack_preconditions": ["<string>", "..."],
  "entry_points": [
    {
      "symbol": "<string>",
      "file": "<string>",
      "kind": "<controller_action|minimal_api_handler|middleware|background_service|signalr_hub|deserializer|api_surface|other>",
      "reason": "<why this is attacker-reachable or externally reachable>"
    }
  ],
  "input_sources": [
    {
      "expression": "<code expression or source>",
      "file": "<path>",
      "kind": "<http_body|query|route|header|cookie|json|form|stream|file|message|other>",
      "taint_role": "<attacker_controlled|derived|validated>"
    }
  ],
  "security_sinks": [
    {
      "expression": "<sink expression>",
      "file": "<path>",
      "kind": "<reflection|process_exec|sql_query|template_render|file_write|path_join|deserializer|crypto_use|unsafe_memory|redirect|other>",
      "reason": "<why this sink matters>"
    }
  ],
  "sanitizers_or_guards": [
    {
      "symbol": "<string>",
      "file": "<path>",
      "kind": "<validator|sanitizer|guard|encoder|normalizer|authorization_check|schema_check>",
      "status": "<present|missing|insufficient|bypassed>",
      "reason": "<why>"
    }
  ],
  "call_chain": [
    {
      "function": "<string>",
      "file": "<string>",
      "role": "<entry|caller|callee|sink|helper>"
    }
  ],
  "control_flow_summary": "<string>",
  "data_flow_summary": "<string>",
  "async_flow_summary": "<string>",
  "patch_intent": {
    "category": "<input_validation|bounds_check|null_guard|state_reset|path_normalization|encoding_fix|auth_fix|permission_fix|resource_limit|async_ordering|deserialization_guard|other>",
    "primary_effect": "<short sentence>",
    "behavior_change": "<what changes semantically>"
  },
  "negative_evidence": ["<rejected hypothesis>", "..."],
  "resolved_identifiers": [
    {
      "name": "<identifier>",
      "kind": "<method|constructor|lambda|delegate|property|field|class|record|struct|interface|enum|attribute|namespace|using|constant|other>",
      "definition": "<actual code definition or full callable body>",
      "function_body": "<full callable body when callable kind; otherwise empty string>",
      "state": "<pre|post|both>",
      "location": "<file:line or file range>",
      "relevance": "<why this identifier matters>"
    }
  ],
  "unresolved_identifiers": ["<identifier>", "..."],
  "patch": {
    "files": ["<path>", "..."],
    "diff": "<full unified diff>",
    "scope_summary": "<short summary of patch scope>"
  },
  "patch_relevance": {
    "security_fix_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk directly fixes the vulnerability>"
      }
    ],
    "supporting_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk supports but does not directly implement the fix>"
      }
    ],
    "unrelated_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk is not vulnerability-relevant>"
      }
    ]
  },
  "validation": {
    "grounded_in_commit": <true|false>,
    "patch_matches_post_state": <true|false>,
    "analysis_matches_pre_state": <true|false>
  },
  "context_quality": {
    "callees_resolved": <int>,
    "types_resolved": <int>,
    "namespaces_resolved": <int>,
    "callers_found": <int>,
    "unresolved": <int>,
    "coverage_score": <float_0_to_1>,
    "method": "opencode_glm5_hybrid_semantic"
  },
  "validator_quality": "high",
  "schema_version": "canonical_v2_csharp"
}

====================
NORMALIZATION RULES
====================
- `primary_symbol` should be the main vulnerable callable or symbol whose behavior best represents the vulnerability
- `helper_symbols_pre` and `helper_symbols_post` must include only directly relevant helper symbols from their respective states
- `helper_symbol_pairs` should map helper identity across states and encode unchanged/modified/renamed/added/removed transitions
- `cwe` must be a single normalized string, not an array
- `severity` must be a single normalized string
- `vulnerable_line_ranges` must be inclusive pre-fix ranges in the form `[[start_line, end_line], ...]`
- `resolved_identifiers` should be concise but sufficient to reconstruct reasoning
- `unresolved_identifiers` must list any symbols still not grounded after exhaustive search
- If a detail is uncertain, prefer explicit uncertainty in the relevant text field rather than hallucination
- `primary_vulnerable_region.lines` should normally align with `vulnerable_line_ranges`
- `security_fix_hunks` should capture direct vulnerability-bearing changes only, not the full patch by default
- If the primary vulnerable callable is anonymous or compiler-generated, use a stable synthetic name in `primary_symbol`, grounded by location or binding context

====================
INTERNAL WORKFLOW YOU MUST FOLLOW
====================
Before producing the JSON:
1. Determine the parent of `{commit_hash}` and set it as `pre_state`
2. Inspect the fix commit and determine the true patch scope
3. Identify the primary vulnerable symbol in `{source_file}` or the touched file set
4. Extract the complete pre-fix callable body for the primary vulnerable target from `pre_state`
5. Extract the complete post-fix callable body for the primary vulnerable target from `post_state`
6. Inspect the surrounding implementation and directly relevant helpers
7. Resolve relevant namespaces, using directives, classes, records, structs, interfaces, methods, lambdas, delegates, properties, attributes, validators, serializers, DI registrations, middleware, and helper bodies from the repository
8. Recover the exploitability-relevant call chain
9. Recover externally reachable entry points
10. Recover attacker-controlled input sources and security-relevant sinks
11. Identify relevant validators, sanitizers, guards, authorization checks, or missing checks
12. Identify the root-cause-bearing pre-fix region
13. Partition all changed hunks into `security_fix_hunks`, `supporting_hunks`, and `unrelated_hunks`
14. Verify that the vulnerability explanation matches the pre-fix code
15. Verify that the patch matches the post-fix code
16. Count unresolved identifiers and set both `unresolved_identifiers` and `context_quality.unresolved`
17. Verify the final JSON is parseable

====================
FINAL SELF-CHECK
====================
Do not output until all are true:
1. `pre_state` is the real parent of `{commit_hash}`
2. `primary_symbol` is the true primary vulnerable symbol
3. `patch.diff` matches the audited fix scope
4. `context_quality.unresolved` equals the number of items in `unresolved_identifiers`
5. `primary_symbol_pre` is present, non-empty, and contains a complete callable body with opening and closing braces, or an exact valid lambda/delegate/property-accessor form
6. `primary_symbol_post` is present, non-empty, and contains a complete callable body with opening and closing braces, or an exact valid lambda/delegate/property-accessor form
7. `vulnerable_line_ranges` is present and uses inclusive `[[start,end], ...]` pairs
8. Every `resolved_identifier` with callable kinds has a `definition` field containing a complete callable body, not just a signature
9. Every `resolved_identifier` with callable kinds has `function_body` present and non-empty
10. No `definition` field in `resolved_identifiers` contains plain English instead of code
11. All code definitions are syntactically plausible C# source code
12. `primary_vulnerable_region` is present and refers only to the true root-cause-bearing pre-fix region
13. `patch_relevance.security_fix_hunks`, `patch_relevance.supporting_hunks`, and `patch_relevance.unrelated_hunks` are all present
14. No changed hunk that is clearly unrelated is mislabeled as a security fix hunk
15. `entry_points`, `input_sources`, `security_sinks`, and `sanitizers_or_guards` are present, even if empty
16. The final JSON is valid and complete


====================
FILE PERSISTENCE SELF-CHECK
====================
Do not finish until all are true:
- `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` exists and is valid JSON.
- The saved file contains the complete canonical JSON object.

Now write the final canonical JSON object to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`. After successful save, output exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.