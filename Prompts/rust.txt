You are generating EXACTLY ONE canonical vulnerability record from audited repository history for a Rust codebase.

Your job is to recover repository-grounded facts from the audited commit and surrounding code, not to generate a chat conversation.

You must:
1. inspect the pre-fix and post-fix repository state,
2. identify the primary vulnerable Rust item,
3. resolve all identifiers necessary to explain exploitability and the fix,
4. recover inter-procedural context and data/control-flow,
5. reconstruct the full real fix scope,
6. extract the complete pre-fix and post-fix primary item bodies,
7. untangle vulnerability-relevant hunks from supporting or unrelated hunks,
8. output exactly one valid JSON object following the schema below.

Treat the CVE and commit as grounding metadata, not as proof of the exact bug mechanism.
Do not invent identifiers, helper functions, locations, definitions, vulnerable regions, or patch hunks.

====================
INPUT ROW
====================
- cve_id: {CVE_ID}
- commit_hash: {COMMIT_HASH}
- selection_source: {SELECTION_SOURCE}
- repo_name: {REPO_NAME}
- repo_url: {REPO_URL}
- commit_urls: {COMMIT_URLS}
- source_file: {SOURCE_FILE}

====================
RUST-SPECIFIC REPOSITORY ANALYSIS REQUIREMENTS
====================
- Resolve all identifiers required for exploitability or patch understanding:
  - free functions
  - inherent methods
  - trait methods
  - associated functions
  - traits and impl blocks
  - structs, enums, unions, type aliases
  - module-level consts and statics
  - associated consts and associated types when relevant
  - declarative macros, derive-generated items, and proc-macro-generated APIs where relevant
  - imported, re-exported, module-defined, or external-crate identifiers from `use`
  - generic parameters, trait bounds, where-clauses, and lifetimes when they matter to correctness
  - unsafe blocks, unsafe functions, unsafe traits, unsafe impls, raw pointers, FFI items, and transmute-like operations when relevant
- Recover the caller/callee chain relevant to exploitability
- Recover attacker-controlled input path where relevant
- Recover patch scope across all changed files/hunks
- If a symbol is still unresolved after exhaustive repository search, keep it in `unresolved_identifiers`
- Prefer exact repository-grounded definitions over paraphrases
- Prefer exact patch fidelity over explanatory creativity
- Do not over-resolve unrelated symbols; only include identifiers material to exploitability, control flow, data flow, ownership/borrowing behavior, panic behavior, type constraints, unsafe behavior, or the fix

====================
RUST IDENTIFIER DEFINITION RULES
====================
- Every identifier with kind = "function" or "method" MUST include the COMPLETE function or method body (from signature through closing brace) as the `definition` value.
- The `definition` field for functions and methods MUST be valid Rust source code, not plain English.
- The `definition` field for macros, consts, statics, structs, enums, unions, type aliases, traits, and impls MUST be the actual code definition, not an English description.
- `vulnerable_function_pre` is a top-level attribute containing the full verbatim body of the primary vulnerable Rust function or method as it existed BEFORE the fix, including the vulnerable line(s).
- `vulnerable_function_post` is a top-level attribute containing the full verbatim body of the primary vulnerable Rust function or method as it existed AFTER the fix, showing the patched line(s).
- Both `vulnerable_function_pre` and `vulnerable_function_post` must be complete, syntactically valid Rust function or method bodies extracted from the repository at `pre_state` and `post_state` respectively.
- If the primary vulnerable item is a method inside an `impl`, preserve the full method body and identify the function using a stable name such as `TypeName::method_name` or `<Type as Trait>::method_name` where appropriate.

====================
PATCH UNTANGLING RULES
====================
- Do not assume every changed hunk in the fix commit is vulnerability-relevant.
- Separate the patch into:
  1. `security_fix_hunks`
  2. `supporting_hunks`
  3. `unrelated_hunks`
- A hunk is `security_fix_hunk` only if it directly changes the vulnerable condition, exploitability path, bounds check, ownership/borrowing correctness, lifetime-sensitive logic, panic condition, unwrap/expect handling, parser validation, deserialization validation, unsafe precondition, pointer validity inside unsafe/FFI code, integer/length validation, authorization condition, path validation, archive extraction validation, or other root-cause-bearing logic.
- A hunk is `supporting_hunk` if it is required for the security fix to compile, preserve invariants, keep borrow/type correctness, update dependent trait/impl logic, adjust error propagation, or propagate the fix safely, but is not itself the primary vulnerable region.
- A hunk is `unrelated_hunk` if it is test-only, formatting-only, rustfmt-only, clippy-only, comment-only, logging-only, rename-only, refactor-only, or otherwise not required to explain or fix the vulnerability.
- `vulnerable_lines` must refer only to the true pre-fix vulnerable region in the primary vulnerable file, not every touched line in the commit.
- Preserve the full patch diff in `patch.diff`, but separately identify which hunks are vulnerability-relevant.
- If a changed file contains both relevant and irrelevant hunks, do not mark the entire file as vulnerable; isolate the specific hunk or region.
- If uncertainty remains about whether a hunk is supporting vs unrelated, prefer `supporting_hunk` and explain the reason in the hunk annotation.

====================
RUST VULNERABILITY ANALYSIS FOCUS
====================
When applicable, analyze whether the vulnerability involves one or more of:
- panic-triggered denial of service
- unchecked indexing or slicing
- integer overflow, underflow, truncation, or lossy cast
- length, offset, or capacity mismatches
- deserialization or parsing flaws
- invalid UTF-8 or string-boundary assumptions
- path traversal or path canonicalization failures
- symlink, archive extraction, or file-permission issues
- misuse of `unsafe`
- raw pointer dereference
- FFI contract violations
- use-after-free, double free, or invalid aliasing through unsafe or FFI boundaries
- lifetime or aliasing violations that become unsound in unsafe code
- misuse of `unwrap`, `expect`, `panic!`, `unreachable!`, `debug_assert!`, or `assert!` on attacker-controlled state
- incorrect `Option` or `Result` handling
- authorization or validation bypass
- resource exhaustion, infinite loop, or unbounded allocation
- concurrency or sync issues involving `Send`, `Sync`, atomics, locking, or shared mutation when relevant

====================
STRICT OUTPUT RULES
====================
- Output VALID JSON only
- No markdown fences
- No prose outside JSON
- No extra top-level keys
- `unresolved_identifiers` must be an array, possibly empty
- `patch.diff` must be the complete unified diff for the true fix scope
- Use normalized severity:
  LOW | MEDIUM | HIGH | CRITICAL | NONE | UNKNOWN
- Use `vulnerable_lines` as an array of integers when the primary vulnerable file/item is clear
- If the true fix spans multiple files, include all of them in `patch.files`
- `vulnerable_function_pre` must be a non-empty string containing the complete pre-fix function or method body
- `vulnerable_function_post` must be a non-empty string containing the complete post-fix function or method body
- All `resolved_identifiers[].definition` fields must contain actual Rust code definitions, not English descriptions
- `primary_vulnerable_region` must refer to the root-cause-bearing pre-fix code region only
- `patch_relevance` must be present even if one or more of its arrays are empty

====================
OUTPUT SCHEMA
====================
{
  "id": "<cve>__<file_path_sanitized>__<primary_item>",
  "cve_id": "<string>",
  "repo": "<string>",
  "repo_url": "<string>",
  "selection_source": "<string>",
  "language": "Rust",
  "crate_name": "<crate name>",
  "module_path": "<crate::module::submodule or UNKNOWN>",
  "file_path": "<primary vulnerable file path>",
  "item_kind": "<function|method|trait_method|async_function|unsafe_function|const_function|macro|other>",
  "primary_function": "<primary vulnerable function or method name>",
  "receiver_type": "<type name or null>",
  "impl_context": {
    "self_type": "<type name or null>",
    "trait": "<trait name or null>"
  },
  "helper_functions": ["<function_or_method>", "..."],
  "commit": "<fix commit sha>",
  "pre_state": "<parent commit sha>",
  "post_state": "<fix commit sha>",
  "rust_edition": "<2015|2018|2021|2024|unknown>",
  "feature_gates": ["<cfg/feature>", "..."],
  "cwe": "<CWE-ID or UNKNOWN>",
  "severity": "<LOW|MEDIUM|HIGH|CRITICAL|NONE|UNKNOWN>",
  "vulnerable": <true|false>,
  "vulnerable_lines": [<int>, "..."],
  "vulnerable_function_pre": "<full function or method body from pre-state>",
  "vulnerable_function_post": "<full function or method body from post-state>",
  "primary_vulnerable_region": {
    "file": "<path>",
    "lines": [<int>, "..."],
    "reason": "<precise explanation of why this is the root-cause-bearing pre-fix region>"
  },
  "root_cause": "<precise repository-grounded explanation>",
  "impact": "<short impact summary>",
  "exploitability_summary": "<short exploitability summary>",
  "attack_preconditions": ["<string>", "..."],
  "call_chain": [
    {
      "function": "<string>",
      "file": "<string>",
      "role": "<entry|caller|callee|sink|helper>"
    }
  ],
  "unsafe_context": {
    "in_unsafe_fn": <true|false>,
    "has_unsafe_block": <true|false>,
    "unsafe_call_chain": ["<function_or_method>", "..."]
  },
  "ffi_boundary": {
    "uses_extern": <true|false>,
    "calls_foreign_code": <true|false>,
    "foreign_symbols": ["<symbol>", "..."]
  },
  "panic_behavior": {
    "can_panic": <true|false>,
    "panic_sites": [<int>, "..."],
    "panic_mechanisms": ["<unwrap|expect|index|panic!|unreachable!|assert!|other>", "..."]
  },
  "error_handling_style": "<Result|Option|panic|mixed|unknown>",
  "async_context": {
    "is_async": <true|false>,
    "await_points": [<int>, "..."]
  },
  "public_entrypoints": ["<pub item>", "..."],
  "control_flow_summary": "<string>",
  "data_flow_summary": "<string>",
  "borrowing_aliasing_notes": "<string>",
  "dependency_context": {
    "external_crates": ["<crate>", "..."],
    "relevant_traits": ["<trait>", "..."]
  },
  "resolved_identifiers": [
    {
      "name": "<identifier>",
      "kind": "<function|method|trait|trait_method|impl|macro|struct|enum|union|type_alias|const|static|field|module|associated_const|associated_type|other>",
      "definition": "<actual Rust code definition or full function/method body>",
      "location": "<file:line or file range>",
      "relevance": "<why this identifier matters>"
    }
  ],
  "unresolved_identifiers": ["<identifier>", "..."],
  "patch": {
    "files": ["<path>", "..."],
    "diff": "<full unified diff>",
    "scope_summary": "<short summary of patch scope>"
  },
  "patch_relevance": {
    "security_fix_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk directly fixes the vulnerability>"
      }
    ],
    "supporting_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk supports but does not directly implement the fix>"
      }
    ],
    "unrelated_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk is not vulnerability-relevant>"
      }
    ]
  },
  "validation": {
    "grounded_in_commit": <true|false>,
    "patch_matches_post_state": <true|false>,
    "analysis_matches_pre_state": <true|false>
  },
  "context_quality": {
    "callees_resolved": <int>,
    "types_resolved": <int>,
    "macros_resolved": <int>,
    "callers_found": <int>,
    "unresolved": <int>,
    "coverage_score": <float_0_to_1>,
    "method": "opencode_glm5_hybrid_semantic"
  },
  "validator_quality": "high",
  "schema_version": "canonical_rust_v2"
}

====================
NORMALIZATION RULES
====================
- `primary_function` should be the main function or method whose behavior best represents the vulnerability
- Use stable Rust naming for methods where helpful, e.g. `Type::method` or `<Type as Trait>::method`
- `helper_functions` should include only directly relevant functions or methods
- `cwe` must be a single normalized string, not an array
- `severity` must be a single normalized string
- `vulnerable_lines` must be line numbers from the pre-fix state for the primary vulnerable file
- `resolved_identifiers` should be concise but sufficient to reconstruct reasoning
- `unresolved_identifiers` must list any symbols still not grounded after exhaustive search
- If a detail is uncertain, prefer explicit uncertainty in the relevant text field rather than hallucination
- `primary_vulnerable_region.lines` should normally be a subset of or equal to `vulnerable_lines`
- `security_fix_hunks` should capture direct vulnerability-bearing changes only, not the full patch by default

====================
INTERNAL WORKFLOW YOU MUST FOLLOW
====================
Before producing the JSON:
1. Determine the parent of `{COMMIT_HASH}` and set it as `pre_state`
2. Inspect the fix commit and determine the true patch scope
3. Identify the primary vulnerable function or method in `{SOURCE_FILE}` or the touched file set
4. Determine the crate name and module path for the primary item
5. Determine whether the primary item is a free function, method, trait method, async fn, const fn, unsafe fn, or macro-like callable
6. Extract the complete pre-fix function or method body for the primary vulnerable item from `pre_state`
7. Extract the complete post-fix function or method body for the primary vulnerable item from `post_state`
8. Inspect the surrounding implementation and directly relevant helpers, traits, impls, macros, type definitions, consts, and statics
9. Resolve relevant macros, types, structs, enums, consts, statics, traits, impl methods, and helper bodies from the repository
10. Recover the exploitability-relevant call chain
11. Identify the root-cause-bearing pre-fix region
12. Determine whether panic behavior, unsafe context, FFI boundaries, feature gates, async behavior, or borrowing/aliasing constraints materially affect exploitability
13. Partition all changed hunks into `security_fix_hunks`, `supporting_hunks`, and `unrelated_hunks`
14. Verify that the vulnerability explanation matches the pre-fix code
15. Verify that the patch matches the post-fix code
16. Count unresolved identifiers and set both `unresolved_identifiers` and `context_quality.unresolved`
17. Verify the final JSON is parseable

====================
FINAL SELF-CHECK
====================
Do not output until all are true:
1. `pre_state` is the real parent of `{COMMIT_HASH}`
2. `primary_function` is the true primary vulnerable Rust function or method
3. `patch.diff` matches the audited fix scope
4. `context_quality.unresolved` equals the number of items in `unresolved_identifiers`
5. `vulnerable_function_pre` is present, non-empty, and contains a complete Rust function or method body with opening and closing braces
6. `vulnerable_function_post` is present, non-empty, and contains a complete Rust function or method body with opening and closing braces
7. Every `resolved_identifier` with kind = "function" or "method" has a `definition` field containing a complete body, not just a signature
8. No `definition` field in `resolved_identifiers` contains plain English instead of code
9. All code definitions are syntactically plausible Rust source code
10. `primary_vulnerable_region` is present and refers only to the true root-cause-bearing pre-fix region
11. `patch_relevance.security_fix_hunks`, `patch_relevance.supporting_hunks`, and `patch_relevance.unrelated_hunks` are all present
12. No changed hunk that is clearly unrelated is mislabeled as a security fix hunk
13. The final JSON is valid and complete

Now produce the final canonical JSON object only.