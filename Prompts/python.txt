You are generating EXACTLY ONE canonical vulnerability record from audited repository history.

Your job is to recover repository-grounded facts from the audited commit and surrounding code, not to generate a chat conversation.

You must:
1. inspect the pre-fix and post-fix repository state,
2. identify the primary vulnerable executable unit,
3. resolve all identifiers necessary to explain exploitability and the fix,
4. recover inter-procedural context and data/control-flow,
5. recover exception-flow, import-resolution, object lifecycle, and framework/runtime context where relevant,
6. reconstruct the full real fix scope,
7. extract the complete pre-fix and post-fix primary vulnerable unit bodies,
8. untangle vulnerability-relevant hunks from supporting or unrelated hunks,
9. output exactly one valid JSON object following the schema below.

Treat the CVE and commit as grounding metadata, not as proof of the exact bug mechanism.
Do not invent identifiers, helper functions, locations, definitions, vulnerable regions, or patch hunks.

====================
INPUT ROW
====================
- cve_id: {cve_id}
- commit_hash: {commit_hash}
- selection_source: {selection_source}
- repo_name: {repo_name}
- repo_url: {repo_url}
- commit_urls: {commit_urls}
- source_file: {source_file}

====================
DETERMINISTIC PATH RULES
====================
- Use deterministic paths only; never use random, timestamped, or user-home-derived temporary directories.
- Repository clone/checkout path MUST be exactly: `/home/user1/dataset_pipeline/repos/{cve_id}`
- If `/home/user1/dataset_pipeline/repos/{cve_id}` already exists, reuse it instead of cloning into a different path.
- Primary canonical JSON path MUST be exactly: `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`
- The filename must use the CVE ID exactly as provided (example: `CVE-2023-0215.json`).
- Do not write alternate final JSON filenames (no `_output`, `_canonical`, numbered, or random suffixes).
- Do not emit the full JSON object to stdout.

====================
FILE WRITE REQUIREMENTS
====================
- Persist the final canonical JSON to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` before finishing.
- Stdout-only completion is invalid.
- If file writing fails, retry until the write succeeds.
- The saved file must contain the complete canonical JSON object.

====================
REPOSITORY ANALYSIS REQUIREMENTS
====================
- Resolve all identifiers required for exploitability or patch understanding:
  - functions, methods, async functions, async methods
  - classes and nested classes
  - decorators
  - module-level constants and globals
  - imported symbols and aliases
  - attributes, instance fields, class attributes
  - properties, descriptors, and property accessors
  - exceptions and custom exception classes
  - type aliases, typing constructs, annotations, protocols, ABCs, dataclass/model fields when relevant
  - closures, nested functions, generators, comprehensions, and context managers if relevant
- Recover the caller/callee chain relevant to exploitability
- Recover attacker-controlled input path where relevant
- For async code, recover await chains, task scheduling paths, callback/event-loop dispatch, background task creation, and framework async entrypoints where relevant
- Recover framework/runtime context when relevant:
  - route handlers
  - middleware/hooks
  - serializers/deserializers
  - model validators
  - task entrypoints
  - CLI entrypoints
  - import-time execution
- Recover patch scope across all changed files and hunks
- If a symbol is still unresolved after exhaustive repository search, keep it in `unresolved_identifiers`
- Prefer exact repository-grounded definitions over paraphrases
- Prefer exact patch fidelity over explanatory creativity
- Do not over-resolve unrelated symbols; only include identifiers material to exploitability, control flow, data flow, authorization, validation, serialization/deserialization, resource usage, exception behavior, or the fix

====================
PYTHON-SPECIFIC REFINEMENTS
====================
- Add `primary_qualified_name` as the fully qualified symbol path:
  - `<module>.<function>`
  - `<module>.<Class>.<method>`
  - `<module>.<Class>.<property>`
  - or the closest accurate executable-unit path available from repository context
- Add `primary_unit_kind` to classify the vulnerable executable unit:
  - `function`
  - `method`
  - `async_function`
  - `async_method`
  - `property_getter`
  - `property_setter`
  - `descriptor_method`
  - `class_body`
  - `module_init`
  - `other`
- The primary vulnerable unit may be a:
  - function
  - method
  - async function
  - async method
  - property accessor
  - descriptor method
  - class body
  - module import side effect
  - or other executable Python code unit
- For attributes, instance fields, and class attributes, resolve the most relevant assignment site, descriptor/property definition, dataclass field declaration, or class-body declaration that explains exploitability
- If a decorator materially changes control flow, authorization, validation, serialization, exception handling, transaction scope, caching, async behavior, retries, dependency injection, or resource management, include it in `resolved_identifiers` and explain its effect in the analysis
- If decorators are attached to the primary vulnerable unit, include them in both `vulnerable_function_pre` and `vulnerable_function_post`, and include their lines in vulnerable ranges when relevant
- For classes, include the class header and the minimal body required to explain the relevant methods, inheritance, decorators, descriptors, dataclass/model fields, mixins, or MRO effects
- Preserve indentation exactly enough for extracted code to remain valid Python source
- Preserve `async def` exactly where applicable
- Prefer runtime-relevant definitions over static typing-only artifacts when the two differ in explanatory value
- If dynamic import, monkeypatching, plugin loading, or descriptor binding affects exploitability, capture that in the appropriate summary fields

====================
IDENTIFIER DEFINITION RULES
====================
- Every identifier with kind = `function`, `method`, `async_function`, `async_method`, `property_getter`, `property_setter`, or `descriptor_method` MUST include the COMPLETE executable body as the `definition` value, not just a declaration or signature
- Every identifier with those executable-unit kinds MUST include `function_body`, and it MUST contain the COMPLETE body as valid Python source code
- The `definition` field for executable units MUST be valid Python source code, not plain English
- The `definition` field for classes, decorators, constants, imports, aliases, exceptions, type aliases, dataclass/model fields, and attributes MUST be the actual code definition or the most relevant code-origin site, not an English description
- `vulnerable_function_pre` is a top-level attribute containing the full verbatim body of the primary vulnerable executable unit as it existed BEFORE the fix, including the vulnerable line(s)
- `vulnerable_function_post` is a top-level attribute containing the full verbatim body of the primary vulnerable executable unit as it existed AFTER the fix, showing the patched line(s)
- Both `vulnerable_function_pre` and `vulnerable_function_post` must be complete, syntactically valid Python executable-unit bodies extracted from the repository at `pre_state` and `post_state` respectively
- Every `resolved_identifiers[]` item MUST include `state` with one of `pre`, `post`, or `both`.
- Do not collapse changed helper definitions across states into one entry; emit separate `state = "pre"` and `state = "post"` entries when the code differs.
- Preserve decorators immediately above the executable unit when they are part of the definition
- Preserve `async def` exactly when applicable
- Preserve indentation exactly enough for the extracted executable unit to remain valid Python source

====================
PATCH UNTANGLING RULES
====================
- Do not assume every changed hunk in the fix commit is vulnerability-relevant
- Separate the patch into:
  1. `security_fix_hunks`
  2. `supporting_hunks`
  3. `unrelated_hunks`
- A hunk is `security_fix_hunk` only if it directly changes the vulnerable condition, exploitability path, unsafe state transition, missing validation, boundary check, authorization condition, parser validation, deserialization validation, path validation, recursion/iteration bound, resource exhaustion guard, exception safety, concurrency condition, state consistency, object validity, attribute existence checks, or other root-cause-bearing logic
- A hunk is `supporting_hunk` if it is required for the security fix to preserve runtime behavior, importability, compatibility, framework integration, exception behavior, invariants, or safe propagation of the fix, but is not itself the primary vulnerable region
- A hunk is `unrelated_hunk` if it is test-only, formatting-only, comment/docstring-only, logging-only, refactor-only, rename-only, dead-code-only, or otherwise not required to explain or fix the vulnerability
- `vulnerable_line_ranges` must be compact inclusive ranges for the same pre-fix vulnerable region, formatted as `[[start_line, end_line], ...]`
- Preserve the full patch diff in `patch.diff`, but separately identify which hunks are vulnerability-relevant
- If a changed file contains both relevant and irrelevant hunks, do not mark the entire file as vulnerable; isolate the specific hunk or region
- If uncertainty remains about whether a hunk is supporting vs unrelated, prefer `supporting_hunk` and explain the reason in the hunk annotation

====================
STRICT OUTPUT RULES
====================
- Persist VALID JSON in `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No markdown fences
- After saving the file, stdout must be exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No extra top-level keys
- `unresolved_identifiers` must be an array, possibly empty
- `patch.diff` must be the complete unified diff for the true fix scope
- Use normalized severity:
  LOW | MEDIUM | HIGH | CRITICAL | NONE | UNKNOWN
- Use `vulnerable_line_ranges` as compact inclusive ranges `[[start_line, end_line], ...]` from the same pre-fix vulnerable region
- If the true fix spans multiple files, include all of them in `patch.files`
- `vulnerable_function_pre` must be a non-empty string containing the complete pre-fix executable-unit body
- `vulnerable_function_post` must be a non-empty string containing the complete post-fix executable-unit body
- All `resolved_identifiers[].definition` fields must contain actual code definitions, not English descriptions
- For each `resolved_identifiers[]` item with executable-unit kinds, `function_body` must be present and non-empty
- `primary_vulnerable_region` must refer to the root-cause-bearing pre-fix code region only
- `patch_relevance` must be present even if one or more of its arrays are empty
- Every helper list must be state-specific (`*_pre` for `pre_state`, `*_post` for `post_state`) and must not merge states.
- Every helper listed in `*_pre` or `*_post` must have at least one matching `resolved_identifiers[]` entry with consistent `state`.

====================
OUTPUT SCHEMA
====================
{
  "id": "<cve>__<file_path_sanitized>__<primary_function>",
  "cve_id": "<string>",
  "repo": "<string>",
  "repo_url": "<string>",
  "selection_source": "<string>",
  "language": "Python",
  "file_path": "<primary vulnerable file path>",
  "module_path": "<python module path>",
  "primary_function": "<primary vulnerable executable-unit short name>",
  "primary_qualified_name": "<fully qualified symbol path for the primary vulnerable executable unit>",
  "primary_unit_kind": "<function|method|async_function|async_method|property_getter|property_setter|descriptor_method|class_body|module_init|other>",
  "containing_class": "<class name or empty string>",
  "helper_functions_pre": ["<helper_from_pre_state>", "..."],
  "helper_functions_post": ["<helper_from_post_state>", "..."],
  "helper_function_pairs": [
    {
      "pre": "<helper_from_pre_state_or_empty>",
      "post": "<helper_from_post_state_or_empty>",
      "change": "<unchanged|modified|renamed|added|removed>"
    }
  ],
  "commit": "<fix commit sha>",
  "pre_state": "<parent commit sha>",
  "post_state": "<fix commit sha>",
  "cwe": "<CWE-ID or UNKNOWN>",
  "severity": "<LOW|MEDIUM|HIGH|CRITICAL|NONE|UNKNOWN>",
  "vulnerable": <true|false>,
  "vulnerable_line_ranges": [[<int>, <int>], "..."],
  "vulnerable_function_pre": "<full executable-unit body from the pre-fix version, as verbatim Python source code string>",
  "vulnerable_function_post": "<full executable-unit body from the post-fix version, as verbatim Python source code string>",
  "primary_vulnerable_region": {
    "file": "<path>",
    "lines": [<int>, "..."],
    "reason": "<precise explanation of why this is the root-cause-bearing pre-fix region>"
  },
  "root_cause": "<precise repository-grounded explanation>",
  "impact": "<short impact summary>",
  "exploitability_summary": "<short exploitability summary>",
  "attack_preconditions": ["<string>", "..."],
  "execution_context": {
    "framework": "<django|fastapi|flask|celery|sqlalchemy|pydantic|stdlib|custom|unknown>",
    "entrypoint_type": "<http_handler|middleware|task|cli|deserializer|validator|import_side_effect|background_task|other>",
    "notes": "<short explanation>"
  },
  "call_chain": [
    {
      "function": "<string>",
      "file": "<string>",
      "role": "<entry|caller|callee|sink|helper>"
    }
  ],
  "control_flow_summary": "<string>",
  "data_flow_summary": "<string>",
  "exception_flow_summary": "<string>",
  "import_context_summary": "<string>",
  "state_lifecycle_summary": "<string>",
  "input_model_summary": "<string>",
  "resolved_identifiers": [
    {
      "name": "<identifier>",
      "kind": "<function|method|async_function|async_method|property_getter|property_setter|descriptor_method|class|decorator|constant|import|alias|exception|attribute|module|other>",
      "definition": "<actual Python code definition or full executable-unit body>",
      "function_body": "<full executable-unit body when applicable; otherwise empty string>",
      "state": "<pre|post|both>",
      "location": "<file:line or file range>",
      "relevance": "<why this identifier matters>"
    }
  ],
  "unresolved_identifiers": ["<identifier>", "..."],
  "patch": {
    "files": ["<path>", "..."],
    "diff": "<full unified diff>",
    "scope_summary": "<short summary of patch scope>"
  },
  "patch_relevance": {
    "security_fix_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk directly fixes the vulnerability>"
      }
    ],
    "supporting_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk supports but does not directly implement the fix>"
      }
    ],
    "unrelated_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk is not vulnerability-relevant>"
      }
    ]
  },
  "validation": {
    "grounded_in_commit": <true|false>,
    "patch_matches_post_state": <true|false>,
    "analysis_matches_pre_state": <true|false>
  },
  "context_quality": {
    "callees_resolved": <int>,
    "types_resolved": "<count of classes, base classes, descriptors, dataclass/model fields, protocols, or typing constructs resolved when relevant>",
    "macros_resolved": 0,
    "decorators_resolved": <int>,
    "imports_resolved": <int>,
    "classes_resolved": <int>,
    "callers_found": <int>,
    "unresolved": <int>,
    "coverage_score": <float_0_to_1>,
    "method": "opencode_glm5_hybrid_semantic"
  },
  "validator_quality": "high",
  "schema_version": "canonical_v4_python"
}

====================
NORMALIZATION RULES
====================
- `primary_function` should be the main executable-unit short name whose behavior best represents the vulnerability
- `primary_qualified_name` should be used for disambiguation and downstream rendering
- `helper_functions_pre` and `helper_functions_post` must include only directly relevant helpers from their respective states
- `helper_function_pairs` should map helper identity across states and encode unchanged/modified/renamed/added/removed transitions
- `cwe` must be a single normalized string, not an array
- `severity` must be a single normalized string
- `vulnerable_line_ranges` must be inclusive pre-fix ranges in the form `[[start_line, end_line], ...]`
- `resolved_identifiers` should be concise but sufficient to reconstruct reasoning
- `unresolved_identifiers` must list any symbols still not grounded after exhaustive search
- If a detail is uncertain, prefer explicit uncertainty in the relevant text field rather than hallucination
- `primary_vulnerable_region.lines` should normally align with `vulnerable_line_ranges`
- `security_fix_hunks` should capture direct vulnerability-bearing changes only, not the full patch by default
- For class methods, preserve decorators and indentation exactly as they appear in the class block
- For async functions and methods, preserve `async def` exactly as it appears in the source
- For methods and properties, `primary_function` may be the short symbol name, but `primary_qualified_name` must disambiguate the containing class/module path

====================
INTERNAL WORKFLOW YOU MUST FOLLOW
====================
Before producing the JSON:
1. Determine the parent of `{commit_hash}` and set it as `pre_state`
2. Inspect the fix commit and determine the true patch scope
3. Identify the primary vulnerable executable unit in `{source_file}` or the touched file set
4. Extract the complete pre-fix body for the primary vulnerable executable unit from `pre_state`
5. Extract the complete post-fix body for the primary vulnerable executable unit from `post_state`
6. Inspect the surrounding implementation and directly relevant helpers
7. Resolve relevant classes, methods, async methods, decorators, imports, constants, aliases, attributes, exceptions, descriptors, properties, typing constructs, and framework hooks from the repository
8. Recover the exploitability-relevant call chain
9. Recover await chains, task scheduling, callback/event-loop dispatch, and framework routing when relevant
10. Identify the root-cause-bearing pre-fix region
11. Partition all changed hunks into `security_fix_hunks`, `supporting_hunks`, and `unrelated_hunks`
12. Verify that the vulnerability explanation matches the pre-fix code
13. Verify that the patch matches the post-fix code
14. Count unresolved identifiers and set both `unresolved_identifiers` and `context_quality.unresolved`
15. Verify the final JSON is parseable

====================
FINAL SELF-CHECK
====================
Do not output until all are true:
1. `pre_state` is the real parent of `{commit_hash}`
2. `primary_function` is the true primary vulnerable executable-unit short name
3. `primary_qualified_name` correctly disambiguates the vulnerable unit
4. `patch.diff` matches the audited fix scope
5. `context_quality.unresolved` equals the number of items in `unresolved_identifiers`
6. `vulnerable_function_pre` is present, non-empty, and contains a complete executable-unit body
7. `vulnerable_function_post` is present, non-empty, and contains a complete executable-unit body
8. `vulnerable_line_ranges` is present and uses inclusive `[[start,end], ...]` pairs
9. Every executable-unit identifier has a `definition` field containing a complete body, not just a signature
10. Every executable-unit identifier has `function_body` present and non-empty, containing a complete body
11. No `definition` field in `resolved_identifiers` contains plain English instead of code
12. All code definitions are syntactically plausible Python source code with correct indentation
13. `primary_vulnerable_region` is present and refers only to the true root-cause-bearing pre-fix region
14. `patch_relevance.security_fix_hunks`, `patch_relevance.supporting_hunks`, and `patch_relevance.unrelated_hunks` are all present
15. No changed hunk that is clearly unrelated is mislabeled as a security fix hunk
16. Decorators are preserved when relevant
17. `async def` is preserved when relevant
18. The final JSON is valid and complete


====================
FILE PERSISTENCE SELF-CHECK
====================
Do not finish until all are true:
- `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` exists and is valid JSON.
- The saved file contains the complete canonical JSON object.

Now write the final canonical JSON object to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`. After successful save, output exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.