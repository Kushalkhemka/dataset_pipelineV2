You are generating EXACTLY ONE canonical vulnerability record from audited repository history.

Your job is to recover repository-grounded facts from the audited commit and surrounding code, not to generate a chat conversation.

You must:
1. inspect the pre-fix and post-fix repository state,
2. identify the primary vulnerable code unit,
3. resolve all identifiers necessary to explain exploitability and the fix,
4. recover inter-procedural context and data/control-flow,
5. reconstruct the full real fix scope,
6. extract the complete pre-fix and post-fix primary callable bodies,
7. untangle vulnerability-relevant hunks from supporting or unrelated hunks,
8. output exactly one valid JSON object following the schema below.

Treat the CVE and commit as grounding metadata, not as proof of the exact bug mechanism.
Do not invent identifiers, helper functions, locations, definitions, vulnerable regions, or patch hunks.

====================
INPUT ROW
====================
- cve_id: {cve_id}
- commit_hash: {commit_hash}
- selection_source: {selection_source}
- repo_name: {repo_name}
- repo_url: {repo_url}
- commit_urls: {commit_urls}
- source_file: {source_file}

====================
DETERMINISTIC PATH RULES
====================
- Use deterministic paths only; never use random, timestamped, or user-home-derived temporary directories.
- Repository clone/checkout path MUST be exactly: `/home/user1/dataset_pipeline/repos/{cve_id}`
- If `/home/user1/dataset_pipeline/repos/{cve_id}` already exists, reuse it instead of cloning into a different path.
- Primary canonical JSON path MUST be exactly: `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`
- The filename must use the CVE ID exactly as provided (example: `CVE-2023-0215.json`).
- Do not write alternate final JSON filenames (no `_output`, `_canonical`, numbered, or random suffixes).
- Do not emit the full JSON object to stdout.

====================
FILE WRITE REQUIREMENTS
====================
- Persist the final canonical JSON to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` before finishing.
- Stdout-only completion is invalid.
- If file writing fails, retry until the write succeeds.
- The saved file must contain the complete canonical JSON object.

====================
REPOSITORY ANALYSIS REQUIREMENTS
====================
- Resolve all identifiers required for exploitability or patch understanding:
  - packages and imports
  - classes, data classes, sealed classes, sealed interfaces, enums, enum entries
  - objects, companion objects, interfaces, abstract classes
  - top-level functions, member functions, constructors, property accessors
  - extension functions and extension properties
  - suspend functions, coroutine scopes, coroutine builders, dispatchers, structured-concurrency boundaries
  - Flow, StateFlow, SharedFlow, Channel, callback bridges, reactive streams where relevant
  - properties, backing fields, delegated properties, lateinit fields, constants, `const val`, `val`, `var`
  - typealiases, generics, variance, inline/reified functions, lambdas, anonymous functions, function types
  - annotations and annotation parameters
  - serializers/deserializers, parsers, adapters, converters, reflection/classloading paths
  - imported symbols and package-level identifiers
  - synthetic accessors, generated bindings, compiler-generated bridges where relevant
  - nullable platform types from Java interop and any nullability contract mismatches across the Java/Kotlin boundary
- Recover the caller/callee chain relevant to exploitability
- Recover attacker-controlled input path where relevant
- Recover patch scope across all changed files/hunks
- If a symbol is still unresolved after exhaustive repository search, keep it in `unresolved_identifiers`
- Prefer exact repository-grounded definitions over paraphrases
- Prefer exact patch fidelity over explanatory creativity
- Do not over-resolve unrelated symbols; only include identifiers material to exploitability, control flow, data flow, lifecycle behavior, concurrency behavior, or the fix

====================
ANDROID / SERVER KOTLIN ANALYSIS REQUIREMENTS
====================
- For Android code, explicitly resolve and reason about any relevant:
  - Activity / Fragment / Service / BroadcastReceiver / ContentProvider lifecycle interactions
  - Intent / Bundle / Parcelable / Serializable / Binder / AIDL / PendingIntent paths
  - permission checks, exported components, URI handling, IPC boundaries, manifest-implied behavior
  - ViewModel / LiveData / lifecycleScope / viewModelScope / WorkManager / foreground service interactions
  - Room / Retrofit / OkHttp / Moshi / Gson / kotlinx.serialization / WebView / URI parsing paths
  - Compose / navigation / SavedStateHandle / DataStore / ContentResolver / FileProvider paths
- For server Kotlin code, explicitly resolve and reason about any relevant:
  - Ktor / Spring / Micronaut / Vert.x / Netty routing and serialization paths
  - request-body parsing, headers, deserializers, reflection-based binding, auth middleware, DI wiring
  - coroutine cancellation boundaries, blocking calls inside suspend contexts, shared mutable state, dispatcher assumptions
- If Android- or server-specific constructs are not relevant, do not force them into the record.

====================
IDENTIFIER DEFINITION RULES
====================
- Every identifier with kind = "function" MUST include the complete callable definition exactly as it appears in the repository.
- Function definitions may be:
  - block-bodied functions,
  - expression-bodied functions,
  - constructors,
  - init blocks,
  - property getters/setters,
  - lambdas or anonymous functions when they are the true vulnerable executable unit.
- The `definition` field for functions MUST be valid Kotlin source code, not plain English.
- The `definition` field for classes, objects, companion objects, interfaces, enums, typealiases, constants, properties, and annotations MUST be the actual code definition, not an English description.
- `vulnerable_function_pre` is a top-level attribute containing the full verbatim body of the primary vulnerable callable as it existed BEFORE the fix, including the vulnerable line(s).
- `vulnerable_function_post` is a top-level attribute containing the full verbatim body of the primary vulnerable callable as it existed AFTER the fix, showing the patched line(s).
- Both `vulnerable_function_pre` and `vulnerable_function_post` must be complete, syntactically valid Kotlin callable definitions extracted from the repository at `pre_state` and `post_state` respectively.
- Every `resolved_identifiers[]` item MUST include `state` with one of `pre`, `post`, or `both`.
- Do not collapse changed helper definitions across states into one entry; emit separate `state = "pre"` and `state = "post"` entries when the code differs.
- If the primary vulnerable logic is in an initializer block, property accessor, lambda assigned to a property, anonymous object method, or companion object method, extract that exact executable code unit and use the nearest stable callable identifier as `primary_function`.

====================
NULLABILITY / INTEROP RULES
====================
- Pay special attention to Kotlin null-safety mismatches caused by:
  - platform types imported from Java
  - unchecked `!!`
  - unsafe casts (`as`)
  - `as?` flows that are later assumed non-null
  - star projections hiding type safety
  - erased generic types
  - reflection-based construction or invocation
  - deserializer outputs with looser nullability than Kotlin declarations
- When Java interop is relevant, explicitly identify whether the vulnerability depends on Kotlin trusting a platform type that can actually be null at runtime.
- If nullability annotations or generated stubs influence exploitability, resolve them and include them in `resolved_identifiers`.

====================
COROUTINE / CONCURRENCY RULES
====================
- If the code uses coroutines, determine whether exploitability depends on:
  - incorrect dispatcher assumptions
  - cancellation not being propagated
  - race conditions across coroutine scopes
  - shared mutable state accessed across coroutines
  - channel/flow buffering or backpressure
  - lifecycle cancellation gaps (Android)
  - blocking I/O or CPU work inside suspend contexts
- Only mark concurrency context as relevant if it materially affects exploitability or patch behavior.

====================
PATCH UNTANGLING RULES
====================
- Do not assume every changed hunk in the fix commit is vulnerability-relevant.
- Separate the patch into:
  1. `security_fix_hunks`
  2. `supporting_hunks`
  3. `unrelated_hunks`
- A hunk is `security_fix_hunk` only if it directly changes the vulnerable condition, exploitability path, nullability guard, lifecycle guard, permission check, parser validation, bounds check, auth logic, serialization contract, concurrency invariant, resource-lifetime rule, or other root-cause-bearing logic.
- A hunk is `supporting_hunk` if it is required for the security fix to compile, preserve invariants, propagate the fix safely, update dependent logic, or keep behavior coherent, but is not itself the primary vulnerable region.
- A hunk is `unrelated_hunk` if it is test-only, formatting-only, comment-only, logging-only, refactor-only, rename-only, dead-code-only, or otherwise not required to explain or fix the vulnerability.
- `vulnerable_line_ranges` must refer only to the true pre-fix vulnerable region in the primary vulnerable file, not every touched line in the commit.
- Preserve the full patch diff in `patch.diff`, but separately identify which hunks are vulnerability-relevant.
- If a changed file contains both relevant and irrelevant hunks, do not mark the entire file as vulnerable; isolate the specific hunk or region.
- If uncertainty remains about whether a hunk is supporting vs unrelated, prefer `supporting_hunk` and explain the reason in the hunk annotation.

====================
STRICT OUTPUT RULES
====================
- Persist VALID JSON in `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No markdown fences
- After saving the file, stdout must be exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No extra top-level keys
- `unresolved_identifiers` must be an array, possibly empty
- `patch.diff` must be the complete unified diff for the true fix scope
- Use normalized severity:
  LOW | MEDIUM | HIGH | CRITICAL | NONE | UNKNOWN
- Use `vulnerable_line_ranges` as compact inclusive ranges [[start_line, end_line], ...] when the primary vulnerable file/callable is clear
- If the true fix spans multiple files, include all of them in `patch.files`
- `vulnerable_function_pre` must be a non-empty string containing the complete pre-fix callable definition
- `vulnerable_function_post` must be a non-empty string containing the complete post-fix callable definition
- All `resolved_identifiers[].definition` fields must contain actual code definitions, not English descriptions
- For each `resolved_identifiers[]` item with callable kinds, `function_body` must be present and non-empty
- `primary_vulnerable_region` must refer to the root-cause-bearing pre-fix code region only
- `patch_relevance` must be present even if one or more of its arrays are empty
- Every helper list must be state-specific (`*_pre` for `pre_state`, `*_post` for `post_state`) and must not merge states.
- Every helper listed in `*_pre` or `*_post` must have at least one matching `resolved_identifiers[]` entry with consistent `state`.
- Keep `macros_resolved` in `context_quality` for cross-language schema compatibility; for Kotlin it should normally be 0

====================
OUTPUT SCHEMA
====================
{
  "id": "<cve>__<file_path_sanitized>__<primary_function>",
  "cve_id": "<string>",
  "repo": "<string>",
  "repo_url": "<string>",
  "selection_source": "<string>",
  "language": "Kotlin",
  "package_name": "<string>",
  "file_path": "<primary vulnerable file path>",
  "primary_function": "<primary vulnerable callable name>",
  "callable_kind": "<top_level_function|member_function|extension_function|constructor|property_getter|property_setter|init_block|lambda|anonymous_function>",
  "declaring_type": "<string or null>",
  "receiver_type": "<string or null>",
  "suspend": <true|false>,
  "visibility": "<public|internal|protected|private|local|unknown>",
  "helper_functions_pre": ["<helper_from_pre_state>", "..."],
  "helper_functions_post": ["<helper_from_post_state>", "..."],
  "helper_function_pairs": [
    {
      "pre": "<helper_from_pre_state_or_empty>",
      "post": "<helper_from_post_state_or_empty>",
      "change": "<unchanged|modified|renamed|added|removed>"
    }
  ],
  "commit": "<fix commit sha>",
  "pre_state": "<parent commit sha>",
  "post_state": "<fix commit sha>",
  "cwe": "<CWE-ID or UNKNOWN>",
  "severity": "<LOW|MEDIUM|HIGH|CRITICAL|NONE|UNKNOWN>",
  "vulnerable": <true|false>,
  "vulnerable_line_ranges": [[<int>, <int>], "..."],
  "vulnerable_function_pre": "<full callable definition from pre-state>",
  "vulnerable_function_post": "<full callable definition from post-state>",
  "primary_vulnerable_region": {
    "file": "<path>",
    "lines": [<int>, "..."],
    "reason": "<precise explanation of why this is the root-cause-bearing pre-fix region>"
  },
  "root_cause": "<precise repository-grounded explanation>",
  "impact": "<short impact summary>",
  "exploitability_summary": "<short exploitability summary>",
  "attack_preconditions": ["<string>", "..."],
  "nullability_contract": {
    "parameters": {
      "<param_name>": "<nullable|nonnullable|platform|unknown>"
    },
    "return_type": "<nullable|nonnullable|platform|unknown>"
  },
  "java_interop_surfaces": [
    {
      "symbol": "<identifier>",
      "kind": "<platform_type|java_method|java_field|generated_stub|annotation_contract>",
      "reason": "<why it matters>"
    }
  ],
  "execution_surface": {
    "platform": "<android|server|multiplatform|library|unknown>",
    "entrypoints": ["<symbol>", "..."],
    "frameworks": ["<framework>", "..."]
  },
  "serialization_surface": {
    "frameworks": ["<framework>", "..."],
    "relevant_types": ["<type>", "..."]
  },
  "concurrency_surface": {
    "uses_coroutines": <true|false>,
    "uses_flow": <true|false>,
    "dispatchers": ["<name>", "..."],
    "shared_state_symbols": ["<symbol>", "..."],
    "cancellation_sensitive": <true|false>
  },
  "call_chain": [
    {
      "function": "<string>",
      "file": "<string>",
      "role": "<entry|caller|callee|sink|helper>"
    }
  ],
  "control_flow_summary": "<string>",
  "data_flow_summary": "<string>",
  "resolved_identifiers": [
    {
      "name": "<identifier>",
      "kind": "<class|object|companion_object|interface|enum|enum_entry|typealias|function|constructor|property|backing_field|getter|setter|constant|annotation|lambda|other>",
      "definition": "<actual code definition or full callable definition>",
      "function_body": "<full callable definition when kind is callable-like; otherwise empty string>",
      "state": "<pre|post|both>",
      "location": "<file:line or file range>",
      "relevance": "<why this identifier matters>"
    }
  ],
  "unresolved_identifiers": ["<identifier>", "..."],
  "patch": {
    "files": ["<path>", "..."],
    "diff": "<full unified diff>",
    "scope_summary": "<short summary of patch scope>"
  },
  "patch_relevance": {
    "security_fix_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk directly fixes the vulnerability>"
      }
    ],
    "supporting_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk supports but does not directly implement the fix>"
      }
    ],
    "unrelated_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk is not vulnerability-relevant>"
      }
    ]
  },
  "validation": {
    "grounded_in_commit": <true|false>,
    "patch_matches_post_state": <true|false>,
    "analysis_matches_pre_state": <true|false>
  },
  "context_quality": {
    "callees_resolved": <int>,
    "types_resolved": <int>,
    "macros_resolved": <int>,
    "callers_found": <int>,
    "unresolved": <int>,
    "coverage_score": <float_0_to_1>,
    "method": "opencode_glm5_hybrid_semantic"
  },
  "validator_quality": "high",
  "schema_version": "canonical_v3_kotlin_android"
}

====================
NORMALIZATION RULES
====================
- `primary_function` should be the main callable whose behavior best represents the vulnerability
- `helper_functions_pre` and `helper_functions_post` must include only directly relevant helpers from their respective states
- `helper_function_pairs` should map helper identity across states and encode unchanged/modified/renamed/added/removed transitions
- `cwe` must be a single normalized string, not an array
- `severity` must be a single normalized string
- `vulnerable_line_ranges` must be inclusive pre-fix ranges in the form `[[start_line, end_line], ...]`
- `resolved_identifiers` should be concise but sufficient to reconstruct reasoning
- `unresolved_identifiers` must list any symbols still not grounded after exhaustive search
- If a detail is uncertain, prefer explicit uncertainty in the relevant text field rather than hallucination
- `primary_vulnerable_region.lines` should normally align with `vulnerable_line_ranges`
- `security_fix_hunks` should capture direct vulnerability-bearing changes only, not the full patch by default

====================
INTERNAL WORKFLOW YOU MUST FOLLOW
====================
Before producing the JSON:
1. Determine the parent of `{commit_hash}` and set it as `pre_state`
2. Inspect the fix commit and determine the true patch scope
3. Identify the primary vulnerable callable in `{source_file}` or the touched file set
4. Extract the complete pre-fix callable definition for the primary vulnerable callable from `pre_state`
5. Extract the complete post-fix callable definition for the primary vulnerable callable from `post_state`
6. Inspect the surrounding implementation and directly relevant helpers
7. Resolve relevant classes, objects, interfaces, enums, properties, constants, annotations, serializers/deserializers, reflection/classloading paths, coroutine constructs, and helper bodies from the repository
8. Recover the exploitability-relevant call chain
9. Identify the root-cause-bearing pre-fix region
10. Partition all changed hunks into `security_fix_hunks`, `supporting_hunks`, and `unrelated_hunks`
11. Verify that the vulnerability explanation matches the pre-fix code
12. Verify that the patch matches the post-fix code
13. Count unresolved identifiers and set both `unresolved_identifiers` and `context_quality.unresolved`
14. Verify the final JSON is parseable

====================
FINAL SELF-CHECK
====================
Do not output until all are true:
1. `pre_state` is the real parent of `{commit_hash}`
2. `primary_function` is the true primary vulnerable callable
3. `patch.diff` matches the audited fix scope
4. `context_quality.unresolved` equals the number of items in `unresolved_identifiers`
5. `vulnerable_function_pre` is present, non-empty, and contains a complete callable definition
6. `vulnerable_function_post` is present, non-empty, and contains a complete callable definition
7. Every `resolved_identifier` with kind = "function" has a `definition` field containing a complete callable definition, not just a signature
8. Every callable `resolved_identifier` has `function_body` present and non-empty
9. No `definition` field in `resolved_identifiers` contains plain English instead of code
10. All code definitions are syntactically plausible Kotlin source code
11. `primary_vulnerable_region` is present and refers only to the true root-cause-bearing pre-fix region
12. `patch_relevance.security_fix_hunks`, `patch_relevance.supporting_hunks`, and `patch_relevance.unrelated_hunks` are all present
13. No changed hunk that is clearly unrelated is mislabeled as a security fix hunk
14. The final JSON is valid and complete


====================
FILE PERSISTENCE SELF-CHECK
====================
Do not finish until all are true:
- `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` exists and is valid JSON.
- The saved file contains the complete canonical JSON object.

Now write the final canonical JSON object to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`. After successful save, output exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.