You are generating EXACTLY ONE canonical vulnerability record from audited repository history.

Your job is to recover repository-grounded facts from the audited commit and surrounding code, not to generate a chat conversation.

You must:
1. inspect the pre-fix and post-fix repository state,
2. identify the primary vulnerable function or method,
3. resolve all identifiers necessary to explain exploitability and the fix,
4. recover inter-procedural context and control/data flow,
5. reconstruct the full real fix scope,
6. extract the complete pre-fix and post-fix primary function/method bodies,
7. extract tight pre-fix and post-fix vulnerable snippets,
8. untangle vulnerability-relevant hunks from supporting or unrelated hunks,
9. output exactly one valid JSON object following the schema below.

Treat the CVE and commit as grounding metadata, not as proof of the exact bug mechanism.
Do not invent identifiers, helper functions, locations, definitions, vulnerable regions, snippets, call paths, or patch hunks.

====================
INPUT ROW
====================
- cve_id: {cve_id}
- commit_hash: {commit_hash}
- selection_source: {selection_source}
- repo_name: {repo_name}
- repo_url: {repo_url}
- commit_urls: {commit_urls}
- source_file: {source_file}

====================
DETERMINISTIC PATH RULES
====================
- Use deterministic paths only; never use random, timestamped, or user-home-derived temporary directories.
- Repository clone/checkout path MUST be exactly: `/home/user1/dataset_pipeline/repos/{cve_id}`
- If `/home/user1/dataset_pipeline/repos/{cve_id}` already exists, reuse it instead of cloning into a different path.
- Primary canonical JSON path MUST be exactly: `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`
- The filename must use the CVE ID exactly as provided (example: `CVE-2023-0215.json`).
- Do not write alternate final JSON filenames (no `_output`, `_canonical`, numbered, or random suffixes).
- Do not emit the full JSON object to stdout.

====================
FILE WRITE REQUIREMENTS
====================
- Persist the final canonical JSON to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` before finishing.
- Stdout-only completion is invalid.
- If file writing fails, retry until the write succeeds.
- The saved file must contain the complete canonical JSON object.

====================
GO-SPECIFIC REPOSITORY ANALYSIS REQUIREMENTS
====================
- Resolve all identifiers required for exploitability or patch understanding:
  - package-level constants and variables
  - imports and imported symbols
  - structs, embedded fields, and tags if relevant
  - interfaces and concrete implementations
  - methods, receivers, helper functions
  - type aliases, generic type parameters, and instantiated generic helpers if relevant
  - typed constants and iota-based enum-like constants
  - package-level sentinels / exported error values
  - build tags and platform-specific file variants if relevant
- Recover the caller/callee chain relevant to exploitability
- Recover attacker-controlled input path where relevant
- Recover goroutine / channel / mutex / atomic / defer / panic / recover behavior if relevant
- Recover interface dispatch where an interface call hides the concrete implementation
- Recover taint-relevant sources and sinks if applicable
- Recover patch scope across all changed files/hunks
- If a symbol is still unresolved after exhaustive repository search, keep it in `unresolved_identifiers`
- Prefer exact repository-grounded definitions over paraphrases
- Prefer exact patch fidelity over explanatory creativity
- Do not over-resolve unrelated symbols; only include identifiers material to exploitability, control flow, data flow, or the fix

====================
IDENTIFIER DEFINITION RULES
====================
- Every identifier with kind = "function" or "method" MUST include the COMPLETE function or method body (from signature through closing brace) as the `definition` value, not just a declaration or signature.
- The `definition` field for functions and methods MUST be valid Go source code, not plain English.
- The `definition` field for constants, vars, structs, interfaces, type aliases, typed constants, and package-level sentinels MUST be the actual Go code definition, not an English description.
- `vulnerable_function_pre` is a top-level attribute containing the full verbatim body of the primary vulnerable function or method as it existed BEFORE the fix, including the vulnerable line(s).
- `vulnerable_function_post` is a top-level attribute containing the full verbatim body of the primary vulnerable function or method as it existed AFTER the fix, showing the patched line(s).
- `primary_vulnerable_snippet_pre` is a top-level attribute containing the tight vulnerable region from the pre-fix state.
- `primary_vulnerable_snippet_post` is a top-level attribute containing the corresponding tight patched region from the post-fix state.
- All four body/snippet fields must be valid Go source fragments extracted from the repository.
- Every `resolved_identifiers[]` item MUST include `state` with one of `pre`, `post`, or `both`.
- Do not collapse changed helper definitions across states into one entry; emit separate `state = "pre"` and `state = "post"` entries when the code differs.
- If the primary vulnerable unit is a method, preserve full receiver syntax in both body fields and in resolved definitions.

====================
PATCH UNTANGLING RULES
====================
- Do not assume every changed hunk in the fix commit is vulnerability-relevant.
- Separate the patch into:
  1. `security_fix_hunks`
  2. `supporting_hunks`
  3. `unrelated_hunks`
- A hunk is `security_fix_hunk` only if it directly changes the vulnerable condition, exploitability path, nil handling, bounds check, slice/index safety, parser validation, authorization condition, resource lifecycle, panic condition, concurrency invariant, or other root-cause-bearing logic.
- A hunk is `supporting_hunk` if it is required for the security fix to compile, preserve invariants, keep behavior coherent, update dependent logic, or propagate the fix safely, but is not itself the primary vulnerable region.
- A hunk is `unrelated_hunk` if it is test-only, formatting-only, comment-only, logging-only, refactor-only, rename-only, benchmark-only, or otherwise not required to explain or fix the vulnerability.
- `vulnerable_line_ranges` must refer only to the true pre-fix vulnerable region in the primary vulnerable file, not every touched line in the commit.
- Preserve the full patch diff in `patch.diff`, but separately identify which hunks are vulnerability-relevant.
- If a changed file contains both relevant and irrelevant hunks, do not mark the entire file as vulnerable; isolate the specific hunk or region.
- If uncertainty remains about whether a hunk is supporting vs unrelated, prefer `supporting_hunk` and explain the reason in the hunk annotation.

====================
STRICT OUTPUT RULES
====================
- Persist VALID JSON in `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No markdown fences
- After saving the file, stdout must be exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.
- No extra top-level keys
- `unresolved_identifiers` must be an array, possibly empty
- `patch.diff` must be the complete unified diff for the true fix scope
- Use normalized severity:
  LOW | MEDIUM | HIGH | CRITICAL | NONE | UNKNOWN
- Use `vulnerable_line_ranges` as compact inclusive ranges [[start_line, end_line], ...] when the primary vulnerable file/function is clear
- If the true fix spans multiple files, include all of them in `patch.files`
- `vulnerable_function_pre` must be a non-empty string containing the complete pre-fix function or method body
- `vulnerable_function_post` must be a non-empty string containing the complete post-fix function or method body
- `primary_vulnerable_snippet_pre` must be a non-empty string containing the tight pre-fix vulnerable snippet
- `primary_vulnerable_snippet_post` must be a non-empty string containing the tight post-fix patched snippet
- All `resolved_identifiers[].definition` fields must contain actual code definitions, not English descriptions
- For each `resolved_identifiers[]` item with kind = "function" or "method", `function_body` must be present and non-empty
- `primary_vulnerable_region` must refer to the root-cause-bearing pre-fix code region only
- `patch_relevance` must be present even if one or more of its arrays are empty
- Every helper list must be state-specific (`*_pre` for `pre_state`, `*_post` for `post_state`) and must not merge states.
- Every helper listed in `*_pre` or `*_post` must have at least one matching `resolved_identifiers[]` entry with consistent `state`.

====================
OUTPUT SCHEMA
====================
{
  "id": "<cve>__<file_path_sanitized>__<primary_function>",
  "cve_id": "<string>",
  "repo": "<string>",
  "repo_url": "<string>",
  "selection_source": "<string>",
  "language": "Go",
  "file_path": "<primary vulnerable file path>",
  "primary_function": "<primary vulnerable function or method name>",
  "primary_symbol_kind": "<function|method>",
  "helper_functions_pre": ["<helper_from_pre_state>", "..."],
  "helper_functions_post": ["<helper_from_post_state>", "..."],
  "helper_function_pairs": [
    {
      "pre": "<helper_from_pre_state_or_empty>",
      "post": "<helper_from_post_state_or_empty>",
      "change": "<unchanged|modified|renamed|added|removed>"
    }
  ],
  "related_symbols": [
    {
      "name": "<symbol>",
      "kind": "<function|method|type|interface|const|var|field|imported_symbol|other>",
      "role": "<helper|callee|caller|sink|source|entry|supporting>"
    }
  ],
  "commit": "<fix commit sha>",
  "pre_state": "<parent commit sha>",
  "post_state": "<fix commit sha>",
  "cwe": "<CWE-ID or UNKNOWN>",
  "severity": "<LOW|MEDIUM|HIGH|CRITICAL|NONE|UNKNOWN>",
  "vulnerable": <true|false>,
  "vulnerable_line_ranges": [[<int>, <int>], "..."],
  "vulnerable_function_pre": "<full pre-fix function or method body as verbatim Go source>",
  "vulnerable_function_post": "<full post-fix function or method body as verbatim Go source>",
  "primary_vulnerable_snippet_pre": "<tight vulnerable snippet from pre-state>",
  "primary_vulnerable_snippet_post": "<tight patched snippet from post-state>",
  "primary_vulnerable_region": {
    "file": "<path>",
    "lines": [<int>, "..."],
    "reason": "<precise explanation of why this is the root-cause-bearing pre-fix region>"
  },
  "root_cause": "<precise repository-grounded explanation>",
  "impact": "<short impact summary>",
  "exploitability_summary": "<short exploitability summary>",
  "attack_preconditions": ["<string>", "..."],
  "entrypoints": [
    {
      "symbol": "<function_or_method>",
      "file": "<path>",
      "kind": "<function|method>",
      "role": "<public_api_entry|internal_entry|decode_entry|request_entry>"
    }
  ],
  "sinks": [
    {
      "symbol": "<symbol>",
      "file": "<path or builtin>",
      "kind": "<function|method|builtin|operation>",
      "role": "<memory_sink|panic_sink|io_sink|auth_sink|parse_sink|other>"
    }
  ],
  "call_chain": [
    {
      "function": "<string>",
      "file": "<string>",
      "role": "<entry|caller|callee|sink|helper>"
    }
  ],
  "interface_dispatch": [
    {
      "interface_symbol": "<interface method or interface type>",
      "concrete_symbol": "<resolved concrete function/method/type>",
      "file": "<path>",
      "reason": "<why this concrete implementation is relevant>"
    }
  ],
  "taint_sources": [
    {
      "symbol": "<source symbol>",
      "file": "<path>",
      "reason": "<why it is attacker-controlled or untrusted>"
    }
  ],
  "taint_sinks": [
    {
      "symbol": "<sink symbol>",
      "file": "<path or builtin>",
      "reason": "<why it is security-relevant>"
    }
  ],
  "control_flow_summary": "<string>",
  "data_flow_summary": "<string>",
  "resolved_identifiers": [
    {
      "name": "<identifier>",
      "kind": "<const|var|struct|interface|type|function|method|field|imported_symbol|other>",
      "definition": "<actual Go code definition or full function/method body>",
      "function_body": "<full function/method body when kind=function|method; otherwise empty string>",
      "state": "<pre|post|both>",
      "location": "<file:line or file range>",
      "relevance": "<why this identifier matters>"
    }
  ],
  "unresolved_identifiers": ["<identifier>", "..."],
  "patch": {
    "files": ["<path>", "..."],
    "diff": "<full unified diff>",
    "scope_summary": "<short summary of patch scope>"
  },
  "patch_relevance": {
    "security_fix_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk directly fixes the vulnerability>"
      }
    ],
    "supporting_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk supports but does not directly implement the fix>"
      }
    ],
    "unrelated_hunks": [
      {
        "file": "<path>",
        "pre_lines": [<int>, "..."],
        "post_lines": [<int>, "..."],
        "reason": "<why this hunk is not vulnerability-relevant>"
      }
    ]
  },
  "tests_touched": ["<path>", "..."],
  "render_hints": {
    "direct_prompt_symbols": ["<field_name>", "..."],
    "agentic_lookup_symbols": ["<symbol_name>", "..."],
    "preferred_detector_scope": "<snippet|function>",
    "preferred_patch_scope": "<snippet|function|full_patch>"
  },
  "validation": {
    "grounded_in_commit": <true|false>,
    "patch_matches_post_state": <true|false>,
    "analysis_matches_pre_state": <true|false>
  },
  "context_quality": {
    "callees_resolved": <int>,
    "types_resolved": <int>,
    "package_symbols_resolved": <int>,
    "interface_impls_resolved": <int>,
    "callers_found": <int>,
    "unresolved": <int>,
    "coverage_score": <float_0_to_1>,
    "method": "opencode_glm5_hybrid_semantic"
  },
  "language_details": {
    "package_name": "<string>",
    "receiver_type": "<string or empty>",
    "receiver_pointer": <true|false>,
    "receiver_name": "<string or empty>",
    "imports_used": ["<import>", "..."],
    "build_tags": ["<tag>", "..."],
    "interface_dispatch_relevant": <true|false>,
    "goroutine_sensitive": <true|false>,
    "panic_path_relevant": <true|false>,
    "defer_sensitive": <true|false>,
    "error_contract_relevant": <true|false>,
    "zero_value_sensitive": <true|false>,
    "cgo_relevant": <true|false>
  },
  "validator_quality": "high",
  "schema_version": "canonical_v3_go"
}

====================
NORMALIZATION RULES
====================
- `primary_function` should be the main function or method whose behavior best represents the vulnerability
- `helper_functions_pre` and `helper_functions_post` must include only directly relevant helpers from their respective states
- `helper_function_pairs` should map helper identity across states and encode unchanged/modified/renamed/added/removed transitions
- `related_symbols` should contain only symbols materially relevant to exploitability or the fix
- `cwe` must be a single normalized string, not an array
- `severity` must be a single normalized string
- `vulnerable_line_ranges` must be inclusive pre-fix ranges in the form `[[start_line, end_line], ...]`
- `resolved_identifiers` should be concise but sufficient to reconstruct reasoning
- `unresolved_identifiers` must list any symbols still not grounded after exhaustive search
- If a detail is uncertain, prefer explicit uncertainty in the relevant text field rather than hallucination
- `primary_vulnerable_region.lines` should normally align with `vulnerable_line_ranges`
- `security_fix_hunks` should capture direct vulnerability-bearing changes only, not the full patch by default
- `receiver_type` should be empty for plain functions
- `receiver_pointer` should be false for plain functions
- `receiver_name` should be empty for plain functions
- `imports_used` should include only imports that materially affect the vulnerable path or patch
- `tests_touched` should include only tests changed in the true fix scope
- `render_hints` should be deterministic and derived from the canonical record, not guessed from downstream formatting preferences

====================
GO-SPECIFIC ANALYSIS NOTES
====================
- Check nil pointer dereferences, slice bounds, map access assumptions, integer overflows in indexing/length arithmetic, panic-driven denial of service, misuse of `copy`, `append`, `unsafe`, `reflect`, `io.Reader` / `io.Writer`, HTTP parsing, archive extraction, path handling, template execution, serialization/deserialization bugs, and concurrency hazards.
- If the vulnerable behavior depends on goroutine interleavings, channels, mutexes, atomics, context cancellation, or deferred cleanup, capture that in `control_flow_summary`, `data_flow_summary`, and `call_chain`.
- If interfaces hide the concrete sink or source, resolve the concrete implementation actually used in the vulnerable path and record it in `interface_dispatch`.
- If build tags or platform-specific files affect the vulnerable path, mention that explicitly.
- For methods, include receiver type details in both `primary_function` reasoning and `resolved_identifiers`.
- Record zero-value sensitivity when the exploitability depends on default-initialized structs, nil maps, nil slices, zero ints, or zero-value state transitions.
- Record error-contract relevance when the bug depends on ignored errors, wrapped errors, sentinel comparisons, or broken return-value assumptions.

====================
INTERNAL WORKFLOW YOU MUST FOLLOW
====================
Before producing the JSON:
1. Determine the parent of `{commit_hash}` and set it as `pre_state`
2. Inspect the fix commit and determine the true patch scope
3. Identify the primary vulnerable function or method in `{source_file}` or the touched file set
4. Extract the complete pre-fix function/method body for the primary vulnerable unit from `pre_state`
5. Extract the complete post-fix function/method body for the primary vulnerable unit from `post_state`
6. Extract tight pre-fix and post-fix vulnerable snippets
7. Inspect the surrounding implementation and directly relevant helpers
8. Resolve relevant package-level constants, vars, types, interfaces, fields, helper signatures/bodies, and imported symbols from the repository
9. Resolve concrete implementations when interface dispatch matters
10. Recover the exploitability-relevant call chain
11. Identify taint sources and sinks if applicable
12. Identify the root-cause-bearing pre-fix region
13. Partition all changed hunks into `security_fix_hunks`, `supporting_hunks`, and `unrelated_hunks`
14. Identify tests touched by the real fix
15. Populate `render_hints` deterministically from the canonical record
16. Verify that the vulnerability explanation matches the pre-fix code
17. Verify that the patch matches the post-fix code
18. Count unresolved identifiers and set both `unresolved_identifiers` and `context_quality.unresolved`
19. Verify the final JSON is parseable

====================
FINAL SELF-CHECK
====================
Do not output until all are true:
1. `pre_state` is the real parent of `{commit_hash}`
2. `primary_function` is the true primary vulnerable function or method
3. `patch.diff` matches the audited fix scope
4. `context_quality.unresolved` equals the number of items in `unresolved_identifiers`
5. `vulnerable_function_pre` is present, non-empty, and contains a complete function or method body with opening and closing braces
6. `vulnerable_function_post` is present, non-empty, and contains a complete function or method body with opening and closing braces
7. `primary_vulnerable_snippet_pre` and `primary_vulnerable_snippet_post` are present and non-empty
8. Every `resolved_identifier` with kind = "function" or "method" has a `definition` field containing a complete function or method body, not just a signature
9. Every `resolved_identifier` with kind = "function" or "method" has `function_body` present and non-empty
10. No `definition` field in `resolved_identifiers` contains plain English instead of code
11. All code definitions are syntactically plausible Go source code
12. `primary_vulnerable_region` is present and refers only to the true root-cause-bearing pre-fix region
13. `patch_relevance.security_fix_hunks`, `patch_relevance.supporting_hunks`, and `patch_relevance.unrelated_hunks` are all present
14. No changed hunk that is clearly unrelated is mislabeled as a security fix hunk
15. `language_details` is present and internally consistent
16. `related_symbols`, `entrypoints`, `sinks`, `interface_dispatch`, `taint_sources`, `taint_sinks`, `tests_touched`, and `render_hints` are all present (they may be empty arrays/objects where appropriate)
17. The final JSON is valid and complete


====================
FILE PERSISTENCE SELF-CHECK
====================
Do not finish until all are true:
- `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json` exists and is valid JSON.
- The saved file contains the complete canonical JSON object.

Now write the final canonical JSON object to `/home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`. After successful save, output exactly: `SAVED /home/user1/dataset_pipeline/cves/{cve_id}__{commit_hash}.json`.